import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
      primary: {
        light: '#fff',
        main: '#fff',
        dark: '#fff',
        contrastText: '#000',
      },
      secondary: {
        light: '#fff',
        main: '#fff',
        dark: '#fff',
        contrastText: '#000',
      },
    },
  });

export { theme }